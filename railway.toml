[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python manage.py collectstatic --noinput && python manage.py migrate && gunicorn elibrary.wsgi:application"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
